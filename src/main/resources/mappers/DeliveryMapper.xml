<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
        "thhp://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.fullstack4.lccbook.mapper.DeliveryMapper">

    <select id="list" resultType="org.fullstack4.lccbook.domain.DeliveryVO">
        select delivery_idx,payment_idx,company_name,company_number,start_date,end_date,delivery_state,delivery_number,recipient_name,recipient_number,addr1,addr2,zip_code,email,memo
        from lcc_delivery
        order by delivery_idx desc
    </select>

    <select id="view" resultType="org.fullstack4.lccbook.domain.DeliveryVO">
        select delivery_idx,payment_idx,company_name,company_number,start_date,end_date,delivery_state,delivery_number,recipient_name,recipient_number,addr1,addr2,zip_code,email,memo
        from lcc_delivery
        where delivery_idx=#{delivery_idx}
    </select>

    <update id="modify">
        update lcc_delivery
        set company_name=#{company_name}, company_number=#{company_number}, start_date=#{start_date}
        , end_date=#{end_date}, delivery_state=#{delivery_state}, delivery_number=#{delivery_number}
        where delivery_idx=#{delivery_idx}
    </update>

    <delete id="delete">
        delete
        from lcc_delivery
        where delivery_idx=#{delivery_idx}
    </delete>

    <select id="bbsTotalCount" resultType="int">
        SELECT COUNT(*) FROM lcc_delivery
    </select>

    <select id="bbsListByPage" resultType="org.fullstack4.lccbook.domain.DeliveryVO">
        select delivery_idx,payment_idx,company_name,company_number,start_date,end_date,delivery_state,delivery_number,recipient_name,recipient_number,addr1,addr2,zip_code,email,memo
        FROM lcc_delivery
        ORDER BY delivery_idx DESC
        LIMIT #{page_skip_count}, #{page_size}
    </select>


</mapper>